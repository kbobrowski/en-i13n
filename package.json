{
  "name": "en-i13n",
  "version": "1.0.0",
  "description": "Tools for working with EN framework",
  "private": true,
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile allow.ts -o allow.js && frida-compile signature.ts -o signature.js && frida-compile patch_e10.ts -o patch_e10.js"
  },
  "devDependencies": {
    "@types/frida-gum": "^16.0.0",
    "@types/node": "^14.0.14",
    "frida-compile": "^9.3.0"
  }
}
